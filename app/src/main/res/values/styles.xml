<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/logo_color</item> 
        <item name="colorPrimaryDark">@color/logo_color_darker</item> 
        <item name="colorControlActivated">@color/control_highlight</item> 
        <item name="colorControlHighlight">@color/control_highlight</item> 
        <item name="colorAccent">@color/accent</item>
    </style>

</resources>
